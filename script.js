// A function that will return all the numbers from zero to the input given
// by a user

function allIntegers(num) {
var numbers = []

    for (var i = 0; i < num; i++) {
        numbers.push(i);
    }
    return numbers;
}

allIntegers()
